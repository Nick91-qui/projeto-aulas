// Importa matchers extras do jest-dom (toBeInTheDocument, etc.)
import "@testing-library/jest-dom";

// A lógica de carregar variáveis de ambiente foi movida para jest.config.ts
// e agora é gerenciada pelo pacote 'dotenv'.
